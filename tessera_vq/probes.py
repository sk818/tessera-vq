"""Downstream linear probes.

Implemented in Phase 5 (docs/spec.md): ``train_linear_probe`` using
``LogisticRegression`` (classification) or ``Ridge`` (regression), with the
regularisation strength tuned on validation. Stub at Phase 0.
"""
