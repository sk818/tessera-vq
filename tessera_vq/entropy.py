"""Entropy coding for index maps: run-length encoding and zstd.

Implemented in Phase 4 (docs/spec.md): ``rle_encode``/``rle_decode`` and
``zstd_compress`` (level 19, via ``zstandard``) with roundtrip tests. Stub at Phase 0.
"""
