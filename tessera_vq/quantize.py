"""Per-tile vector quantisation: k-means codebooks and index maps.

Implemented in Phase 3 (docs/spec.md): ``quantize_tile`` returning a ``(k, 128)`` codebook
and ``(H, W)`` index map, with euclidean/cosine distance variants. Stub at Phase 0.
"""
